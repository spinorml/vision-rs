[model]
name    = "yolo26n"
variant = "n"
nc      = 80
source  = "ultralytics"

[download]
url      = "https://github.com/ultralytics/assets/releases/download/v8.4.0/yolo26n.pt"
filename = "yolo26n.pt"

[weights.mapping]
# Ultralytics parameter key → vision-rs parameter key.
# Populated once weight loading is implemented in load_weights_from_safetensors.
