[model]
name    = "yolo26l"
variant = "l"
nc      = 80
source  = "ultralytics"

[download]
url      = "https://huggingface.co/datasets/teenygrad/ultralytics-yolo26/resolve/main/ultralytics/yolo26/yolo26l.safetensors"
filename = "yolo26l.safetensors"

[weights.mapping]
# Ultralytics parameter key → vision-rs parameter key.
# Populated once weight loading is implemented in load_weights_from_safetensors.
